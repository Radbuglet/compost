[package]
name = "compost"
version = "0.1.1"
description = "decomposes tuples into tuples containing a subset of their values"
readme = "../../README.md"
categories = ["rust-patterns", "no-std"]
license = "MIT"
repository = "https://github.com/Radbuglet/compost"
edition = "2021"

[dependencies]
